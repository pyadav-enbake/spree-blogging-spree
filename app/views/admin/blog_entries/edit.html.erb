<h1>Editing Blog Entry</h1>

<% form_for([:admin, @blog_entry], :html => { :multipart => true }) do |f| %>
  <%= render :partial => f %>
  <p class="form-buttons">
    <%= button t("update"), nil, 'submit', {'class' => 'wymupdate'} %>
    <%= t("or") %> <%= link_to t("cancel"), collection_url %>
  </p>
<% end %>
